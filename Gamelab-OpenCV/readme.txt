firsttime:

1. install python 3.10.5

/* if there is an environment, steps 2 and 3 are not requested
2. create new environment: 
cmd: py -m venv env

3. go to the python script folder and install the requirements:
cmd: py -m pip install -r requirements.txt
*/

each time:

1. activate the environment
cmd: .\env\Scripts\activate

2. run the python script:
cmd: py main.py